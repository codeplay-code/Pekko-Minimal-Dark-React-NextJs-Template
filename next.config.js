/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // basePath: "/pekko-nextjs",
  // assetPrefix: "/pekko-nextjs/",
  trailingSlash: true,
};

module.exports = nextConfig;
